authtoken: {your auth token }
tunnels:
  backend:
    proto: http
    addr: 5200
    host_header: localhost:5200

  frontend:
    proto: http
    addr: 4200
    host_header: localhost:4200


version: 2
